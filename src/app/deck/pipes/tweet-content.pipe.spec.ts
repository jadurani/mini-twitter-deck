import { TweetContentPipe } from './tweet-content.pipe';

describe('TweetContentPipe', () => {
  it('create an instance', () => {
    const pipe = new TweetContentPipe();
    expect(pipe).toBeTruthy();
  });
});
